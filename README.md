# Retail-Analytics-Project
# Aggregation formulas:

COUNTIF, COUNTIFS, AVERAGEIF, IF, Nested IF

# Lookup formulas:

VLOOKUP, INDEX MATCH, XLOOKUP

# Supporting concepts:

Relative referencing (why dragging works)
Absolute referencing ($)
Never hardcode numbers
#N/A error diagnosis
Sanity checks (totals must add up)